# LeoJarvis does not require environment variables for normal local use.
# Runtime configuration lives in config/*.toml and local data/user_settings.json.
# Keep API keys on your machine only; do not commit real secrets.

# Optional MCP / search providers. Leave blank until you add your own keys.
TAVILY_API_KEY=
GITHUB_TOKEN=
AMAP_MAPS_API_KEY=
