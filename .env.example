# Cortex V1 does not require process environment variables for normal local use.
# Runtime configuration lives in config/*.toml.
# Keep API keys in config/models.toml on your machine only; do not commit real secrets.
